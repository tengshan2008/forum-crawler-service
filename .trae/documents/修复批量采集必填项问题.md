## 修复批量采集必填项问题

**问题分析**：

* 在两个Task模型文件（`task.js`和`Task.js`）中，`forumUrl`字段都被设置为`required: true`

* 这导致即使选择了批量采集类型，系统仍然要求填写论坛地址

* 与我们之前设计的动态验证逻辑冲突

**修复方案**：

1. **修改Task模型**：将`forumUrl`字段的`required`属性设置为`false`
2. **保持现有验证逻辑**：后端控制器中的动态验证逻辑已经正确实现，会根据`crawlType`验证必填字段
3. **前端表单验证**：前端表单的动态验证逻辑也已经正确实现
4. **删除重复文件**：解决文件系统大小写敏感问题，删除其中一个重复文件

**修复步骤**：

1. 编辑`/home/pi/Workspace/forum-crawler-service/backend/src/models/Task.js`，将`forumUrl`的`required`改为`false`
2. 编辑`/home/pi/Workspace/forum-crawler-service/backend/src/models/task.js`，将`forumUrl`的`required`改为`false`
3. 解决文件重复问题，删除其中一个文件（建议保留`Task.js`作为主要文件）

**预期效果**：

* 单帖采集时：仅要求填写`forumUrl`

* 批量采集时：仅要求填写`sectionUrl`

* 表单提交时根据选择的采集类型动态验证

* 解决文件系统大小写敏感问题

